# Backend (set these in your Vercel project settings / local backend/.env)
PORT=
DATABASE_URL=
JWT_SECRET=
JWT_EXPIRES_IN=
CORS_ORIGIN=

# Frontend (only needed if the API host differs from same-origin /api)
# VITE_API_URL=